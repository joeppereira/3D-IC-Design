import type { ColorStop } from './types.js';
/**
 * applies `gradient` to `context`
 */
export declare const fillGradient: (context: OffscreenCanvasRenderingContext2D, gradient: CanvasGradient) => void;
/**
 * adds each `stop` of `stops` to the gradient
 */
export declare const addStops: (gradient: CanvasGradient, stops?: ColorStop[]) => CanvasGradient;
